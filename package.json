{
  "name": "idisplay_addon",
  "version": "1.0.0",
  "description": "C++ binding for IDisplay",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David Uche",
  "license": "MIT",
  "devDependencies": {
    "node-gyp": "^5.0.3"
  },
  "dependencies": {
    "node-addon-api": "^1.7.1"
  }
}
